# Stage 4: Cloud Deploy

**Status**: TODO
**Prerequisites**: Local testing complete (Stage 3)

---

## Overview

Deploy the static site or simple service to App Platform.

---

## Pre-Deployment Checklist

- [ ] `.do/app.yaml` exists
- [ ] Build command works locally
- [ ] `deploy_on_push` set appropriately

---

## Deploy

### Option A: Via doctl

```bash
doctl apps create --spec .do/app.yaml
```

### Option B: Via GitHub Actions

```bash
git push origin main
```

- [ ] Deployment started
- [ ] Capture app ID: `APP_ID=`

---

## Monitor Deployment

```bash
APP_ID=$(doctl apps list --format ID,Spec.Name --no-header | grep {{APP_NAME}} | awk '{print $1}')

# Watch build
doctl apps logs $APP_ID --type build --follow

# Watch deploy
doctl apps logs $APP_ID --type deploy --follow

# Check status
doctl apps get $APP_ID --format DefaultIngress,ActiveDeployment.Phase
```

---

## Deployment Result

- [ ] App URL: `https://{{APP_NAME}}-_____.ondigitalocean.app`
- [ ] Build succeeded
- [ ] Deploy succeeded

---

## Troubleshooting

| Issue | Solution |
|-------|----------|
| Build failed | Check build logs, verify build command |
| Deploy failed | Check Dockerfile or buildpack |
| Health check timeout | Verify port configuration |

---

## Next Steps

Proceed to Stage 5: Cloud Validate

# Prompt: document game stack

> **Execution Directive:** Implement via the `fnb-stack-implementor` and `fnb-stack-spec` skills.
> Invoke: `/fnb-stack-spec /fnb-stack-implementor <this-file>`

create a markdown that documents, in human-readable format, how the gamestack all fits together

give a high-level overview of each section up front, with deep detail sections later in the doc that can be linked to

provide sequence diagrams if possible

explain how the machine moves are triggered and executed in detail

provide code snippets, with links to the actual files they come from so i can inspect everything quickly

give me an evaluation section at the end detailing any shortcomings you may find with the stack
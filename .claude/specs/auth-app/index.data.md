# auth-app/index — Entry Redirect Data

## Status
Implemented

## Route
`/auth/` — see `index.ui.md` for behavior

## API
No API calls. Uses `useAuth().isLoggedIn` (cookie-derived) to decide redirect target.

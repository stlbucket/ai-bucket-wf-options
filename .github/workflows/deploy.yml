# Provision/refresh infra + deploy the stack to the box (spec terraform-and-cicd.md §4 +
# tag-auto-deploy.md TD1–TD3). Two entry points: manual dispatch (infra applies, plan previews,
# rollbacks, non-main ships) and workflow_call from build-images.yml (the v*-tag-on-main auto
# path, always mode=deploy-only). The repo owner wires the referenced secrets / AWS OIDC role
# and owns all commits. The assistant never runs git/apply.
name: deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Environment to deploy
        type: choice
        # aws-prod is PARKED (terraform authored, never provisioned) — its branches in this
        # workflow stay wired; re-add the option when the AWS env stands up (plan 0560 item 7).
        options: [do-prod]
        required: true
      image_tag:
        description: Image tag (git SHA) built by build-images
        required: true
      mode:
        description: >-
          deploy-only = terraform outputs only, then deploy (default; rollbacks, code ships) ·
          apply-and-deploy = terraform apply first (infra changes) ·
          plan-only = terraform plan preview, no deploy (prod gate, spec §6)
        type: choice
        options: [deploy-only, apply-and-deploy, plan-only]
        default: deploy-only
  # Called by build-images.yml after a v* tag build passes the main-ancestry guard (TD1/TD4).
  # Same input names as dispatch — both populate the inputs.* context. Caller passes
  # secrets: inherit; its job carries the matching concurrency group (TD6 — workflow-level
  # concurrency below does not bind called runs).
  workflow_call:
    inputs:
      environment:
        description: Environment to deploy (do-prod)
        type: string
        required: true
      image_tag:
        description: Image tag (git SHA) built by build-images
        type: string
        required: true
      mode:
        description: deploy-only | apply-and-deploy | plan-only
        type: string
        default: deploy-only

concurrency:
  group: deploy-${{ inputs.environment }}   # serialize deploys per env (also the practical state lock)
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      id-token: write
      contents: read
    env:
      TF_DIR: infra/terraform/environments/${{ inputs.environment }}
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.10.5   # >= 1.10 for the S3 use_lockfile backend

      # ── Cloud credentials (branch by environment) ──
      - if: inputs.environment == 'do-prod'
        name: DO credentials
        run: |
          echo "DIGITALOCEAN_TOKEN=${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}" >> "$GITHUB_ENV"
          # Spaces key doubles as the state-backend + bucket-resource creds.
          echo "AWS_ACCESS_KEY_ID=${{ secrets.SPACES_ACCESS_KEY_ID }}" >> "$GITHUB_ENV"
          echo "AWS_SECRET_ACCESS_KEY=${{ secrets.SPACES_SECRET_ACCESS_KEY }}" >> "$GITHUB_ENV"
          echo "SPACES_ACCESS_KEY_ID=${{ secrets.SPACES_ACCESS_KEY_ID }}" >> "$GITHUB_ENV"
          echo "SPACES_SECRET_ACCESS_KEY=${{ secrets.SPACES_SECRET_ACCESS_KEY }}" >> "$GITHUB_ENV"
      - if: inputs.environment == 'aws-prod'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      # ── Terraform provision ──
      - name: terraform init
        run: terraform -chdir="$TF_DIR" init -input=false
      - name: terraform apply
        if: inputs.mode == 'apply-and-deploy'
        env:
          TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}   # RDS master (aws-prod); ignored on do-prod
        run: terraform -chdir="$TF_DIR" apply -input=false -auto-approve -var-file="${{ inputs.environment }}.tfvars"
      - name: terraform plan (preview gate — no deploy)
        if: inputs.mode == 'plan-only'
        run: terraform -chdir="$TF_DIR" plan -input=false -var-file="${{ inputs.environment }}.tfvars"

      # ── Export Terraform outputs into the environment for render-env ──
      # Pure `terraform output` — works with or without a fresh apply (TD2: the tag path never
      # applies; it deploys against existing infra state).
      - name: export tf outputs
        if: inputs.mode != 'plan-only'
        run: |
          terraform -chdir="$TF_DIR" output -json > tf.json
          # tf outputs are NOT GitHub secrets — mask the sensitive ones or they print in clear
          # in step-env dumps (first-deploy defect: the PG admin password leaked into run logs).
          for k in pg_admin_password app_pg_password; do
            v=$(jq -r --arg k "$k" '.[$k].value // empty' tf.json); [ -n "$v" ] && echo "::add-mask::$v" || true
          done
          to_env() { v=$(jq -r --arg k "$1" '.[$k].value // empty' tf.json); [ -n "$v" ] && echo "$2=$v" >> "$GITHUB_ENV" || true; }
          to_env registry_endpoint  REGISTRY
          to_env domain             DOMAIN
          to_env pg_host            MANAGED_PG_HOST
          to_env pg_port            MANAGED_PG_PORT
          to_env pg_admin_user      MANAGED_PG_ADMIN_USER
          to_env pg_admin_db        MANAGED_PG_ADMIN_DB
          to_env app_pg_user        APP_PG_USER
          to_env s3_endpoint        S3_ENDPOINT
          to_env s3_public_base_url S3_PUBLIC_BASE_URL
          to_env s3_region          S3_REGION
          to_env s3_bucket          S3_BUCKET
          # DO exposes the PG password as a TF output; AWS uses the DB_PASSWORD secret (below).
          to_env pg_admin_password  MANAGED_PG_ADMIN_PASSWORD
          to_env app_pg_password    APP_PG_PASSWORD
          to_env firewall_id        FIREWALL_ID

      # ── Render the box .env (secrets + tf outputs) and deploy ──
      - name: render .env
        if: inputs.mode != 'plan-only'
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
          # aws-prod PG values come from the DB_PASSWORD secret / a fixed maintenance DB; on
          # do-prod fall back to the tf outputs exported to GITHUB_ENV above (step env overrides
          # GITHUB_ENV, so an empty-string fallback would clobber them — first-deploy defect).
          MANAGED_PG_ADMIN_DB: ${{ inputs.environment == 'aws-prod' && 'fnb' || env.MANAGED_PG_ADMIN_DB }}
          MANAGED_PG_ADMIN_PASSWORD: ${{ inputs.environment == 'aws-prod' && secrets.DB_PASSWORD || env.MANAGED_PG_ADMIN_PASSWORD }}
          APP_PG_PASSWORD: ${{ inputs.environment == 'aws-prod' && secrets.DB_PASSWORD || env.APP_PG_PASSWORD }}
          ACME_EMAIL: ${{ secrets.ACME_EMAIL }}
          ZITADEL_DB_PASSWORD: ${{ secrets.ZITADEL_DB_PASSWORD }}
          N8N_ENGINE_DB_PASSWORD: ${{ secrets.N8N_ENGINE_DB_PASSWORD }}
          N8N_WORKER_PG_PASSWORD: ${{ secrets.N8N_WORKER_PG_PASSWORD }}
          S3_ACCESS_KEY: ${{ secrets.S3_ACCESS_KEY }}
          S3_SECRET_KEY: ${{ secrets.S3_SECRET_KEY }}
          NUXT_SESSION_SECRET: ${{ secrets.NUXT_SESSION_SECRET }}
          ZITADEL_MASTERKEY: ${{ secrets.ZITADEL_MASTERKEY }}
          N8N_ENCRYPTION_KEY: ${{ secrets.N8N_ENCRYPTION_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          N8N_WEBHOOK_SECRET: ${{ secrets.N8N_WEBHOOK_SECRET }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}     # fnb-smtp credential (Resend SMTP)
          # twilio-production-sms spec (PARKED): the tpl doesn't reference these tokens while
          # parked, so missing GH secrets are harmless. Required again when the cutover un-parks.
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
          TWILIO_FROM_NUMBER: ${{ secrets.TWILIO_FROM_NUMBER }}
          SETUP_TOKEN: ${{ secrets.SETUP_TOKEN }}           # first-run /auth/setup gate (§9.1)
          ZITADEL_ADMIN_USERNAME: ${{ secrets.ZITADEL_ADMIN_USERNAME }}
          ZITADEL_ADMIN_EMAIL: ${{ secrets.ZITADEL_ADMIN_EMAIL }}
          ZITADEL_ADMIN_PASSWORD: ${{ secrets.ZITADEL_ADMIN_PASSWORD }}
          MAPBOX_ACCESS_TOKEN: ${{ secrets.MAPBOX_ACCESS_TOKEN }}
        run: node infra/env/render-env.mjs infra/env/.env.prod.tpl "$RUNNER_TEMP/.env"

      # ── Temporary ssh window: the cloud firewall opens 22 to admin CIDRs only, which the
      # runner is not in. Add the runner's IP for the deploy; the close step below removes it
      # (always). A doctl-added rule sits outside terraform state, so even a failed cleanup
      # self-heals at the next apply (the firewall resource's rule set is authoritative).
      - if: inputs.mode != 'plan-only' && inputs.environment == 'do-prod'
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
      - name: open ssh window for the runner
        if: inputs.mode != 'plan-only' && inputs.environment == 'do-prod'
        run: |
          RUNNER_IP="$(curl -fsS https://api.ipify.org)/32"
          echo "RUNNER_IP=$RUNNER_IP" >> "$GITHUB_ENV"
          doctl compute firewall add-rules "$FIREWALL_ID" \
            --inbound-rules "protocol:tcp,ports:22,address:$RUNNER_IP"

      - name: deploy to the box
        if: inputs.mode != 'plan-only'
        env:
          ENVIRONMENT: ${{ inputs.environment }}
          IMAGE_TAG: ${{ inputs.image_tag }}
          ENV_FILE: ${{ runner.temp }}/.env
          AWS_REGION: ${{ secrets.AWS_REGION }}
          SSH_KEY: ${{ secrets.BOX_SSH_PRIVATE_KEY }}   # private key matching the box's authorized key
        run: |
          # BOX_HOST + REGISTRY come from the tf outputs exported above (reserved_ip/eip; registry_endpoint).
          echo "$SSH_KEY" > "$RUNNER_TEMP/id" && chmod 600 "$RUNNER_TEMP/id"
          export SSH_OPTS="-i $RUNNER_TEMP/id -o StrictHostKeyChecking=accept-new"
          export BOX_HOST="$(jq -r '(.reserved_ip // .eip).value' tf.json)"
          infra/scripts/deploy.sh
        # SSH_KEY is the private key matching the box's authorized key (a secret).
      - name: health verify
        if: inputs.mode != 'plan-only'
        run: DOMAIN="$DOMAIN" infra/scripts/health-verify.sh

      - name: close ssh window
        if: always() && env.RUNNER_IP != '' && env.FIREWALL_ID != ''
        run: |
          doctl compute firewall remove-rules "$FIREWALL_ID" \
            --inbound-rules "protocol:tcp,ports:22,address:$RUNNER_IP" || true

node_modules
.turbo
dist
.output
.nuxt
coverage
*.log
.env
.env.*
!.env.example
# the prod template is placeholders only (no secrets) — CI's render step needs it
!.env.prod.tpl
_cruft
.pnpm-store
packages/db-types/src/generated
# generated hooks/types ARE committed (CI image builds can't run codegen — it needs a live
# PostGraphile); only the raw introspection dumps stay ignored
packages/graphql-client-api/src/generated/schema.json
packages/graphql-client-api/src/generated/schema.min.json
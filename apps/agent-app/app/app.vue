<script setup lang="ts">
// Headless app — this page exists only so `nuxt dev`/`nuxt build` have an app entry.
// All real work happens in server/ (trigger routes, agent harness, croner scheduler).
</script>

<template>
  <div>fnb agent-app — headless Claude Agent SDK workflow engine (no UI, not routed through Caddy)</div>
</template>

mutation SetWorkspaceMembership(
  $profileId: UUID!
  $member: Boolean!
) {
  setWorkspaceMembership(input: {
    _profileId: $profileId
    _member: $member
  }) {
    resident {
      ...Resident
    }
  }
}

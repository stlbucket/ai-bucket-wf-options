query SearchProfiles(
  $searchTerm: String
  $limit: Int
  $offset: Int
) {
  searchProfilesList(_options: {
    searchTerm: $searchTerm
    pagingOptions: { itemLimit: $limit, itemOffset: $offset }
  }) {
    ...Profile
  }
  searchProfilesCount(_options: {
    searchTerm: $searchTerm
  })
}

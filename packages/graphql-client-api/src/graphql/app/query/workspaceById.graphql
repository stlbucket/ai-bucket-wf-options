query WorkspaceById(
  $tenantId: UUID!
) {
  tenant(id: $tenantId) {
    ...Tenant
    residents: residentsList {
      ...Resident
      licenses: licensesList {
        ...License
      }
    }
    tenantSubscriptions: tenantSubscriptionsList {
      ...TenantSubscription
    }
  }
}

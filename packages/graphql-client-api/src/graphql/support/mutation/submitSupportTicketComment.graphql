mutation SubmitSupportTicketComment($ticketId: UUID!, $body: String!) {
  submitSupportTicketComment(input: {
    _ticketId: $ticketId
    _body: $body
  }) {
    supportTicketComment {
      ...SupportTicketComment
    }
  }
}
